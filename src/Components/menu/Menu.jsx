import React from 'react';

import './menu.scss'

const Menu = ({items, active, setActive}) => {
    

    return (
        <section className='burgerMenu'>
            
        </section>
    );
};

export default Menu;